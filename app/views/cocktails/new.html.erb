 <%= simple_form_for @cocktail do |f| %>
  <%= f.input :name, hint: 'You cannot change your username.' %>
  <%= f.simple_fields_for :doses do |p| %>
    <%= p.input :description %>
    <%= p.association :ingredient  %>
  <% end %>
  <%= f.button :submit %>
<% end %>

