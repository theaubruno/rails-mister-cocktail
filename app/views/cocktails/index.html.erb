<h1>Cocktails</h1>

<%= link_to 'Create a cocktail', new_cocktail_path %>

<ul>
  <% @cocktails.each do |cocktail| %>
  <h3> <%= link_to cocktail.name, cocktail_path(cocktail) %></h3> <%= link_to 'Supprimer', cocktail_path(cocktail), method: :delete %>
  <% end %>
</ul>

